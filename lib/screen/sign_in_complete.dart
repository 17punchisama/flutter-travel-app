import 'package:flutter/material.dart';

class SignInComplete extends StatelessWidget {
  const SignInComplete({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}